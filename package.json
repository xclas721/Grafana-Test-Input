{
  "name": "grafana-test-input",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "format": "prettier --write \"*.html\"",
    "format:check": "prettier --check \"*.html\"",
    "lint": "prettier --check \"*.html\" || (echo 'Prettier formatting issues detected!' && exit 1)"
  },
  "devDependencies": {
    "prettier": "^3.2.5"
  }
}

